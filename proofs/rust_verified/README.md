# Rust-Verified Proofs

This directory is reserved for Rust-to-Rocq translated proofs using
`rocq-of-rust`. The layout should mirror the Rust modules selected for
verification.

See `proofs/DECISIONS.md` for the spec baseline and toolchain decisions.
